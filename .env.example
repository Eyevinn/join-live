# WHIP Gateway Configuration (for participants sending streams)
# Default: https://eyevinnlab-livevibe.eyevinn-smb-whip-bridge.auto.prod.osaas.io (OSC livevibe service)
# Set this to your Symphony Media Bridge WHIP gateway base URL (without /api/v2/whip/sfu-broadcaster)
# The endpoint path will be added automatically
WHIP_GATEWAY_URL=https://eyevinnlab-livevibe.eyevinn-smb-whip-bridge.auto.prod.osaas.io

# WHEP Gateway Configuration (for editor receiving streams)
# Default: Uses same as WHIP_GATEWAY_URL if not specified
# Set this to your Symphony Media Bridge WHEP gateway base URL
WHEP_GATEWAY_URL=https://eyevinnlab-livevibe.eyevinn-smb-whip-bridge.auto.prod.osaas.io

# WHIP Authentication Key
# Optional: Set this if your WHIP gateway requires authentication
# The key will be passed as authkey to the WHIP client
WHIP_AUTH_KEY=your-auth-key-here

# WHEP Authentication Key
# Optional: Set this if your WHEP gateway requires authentication
# Defaults to WHIP_AUTH_KEY if not specified
WHEP_AUTH_KEY=your-auth-key-here

# Port for the web server
PORT=3000