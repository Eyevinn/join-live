# WHIP Gateway Configuration
# Default: https://livevibe.osaas.io/api/v2/whip/sfu-broadcaster (OSC livevibe service)
# Set this to your Symphony Media Bridge WHIP gateway URL
WHIP_GATEWAY_URL=https://livevibe.osaas.io/api/v2/whip/sfu-broadcaster

# WHIP Authentication Key
# Optional: Set this if your WHIP gateway requires authentication
# The key will be passed as authkey to the WHIP client
WHIP_AUTH_KEY=your-auth-key-here

# Port for the web server
PORT=3000